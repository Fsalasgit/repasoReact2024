const Counter = ({number}) => {

    //El componente cambia cuando cambia el estado interno o le llegan nuevas props

    return(
        <>
            <h1>{number}</h1>
        </>
    )
}

export default Counter